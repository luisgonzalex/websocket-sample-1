# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Client URL (for CORS in development)
# Replace the IP with your machine's LAN IP so phones/tablets can load the Vite dev server
CLIENT_URL=http://192.168.68.60:5173
